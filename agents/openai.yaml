interface:
  display_name: "Mary Workflow"
  short_description: "Run milestone workflows and manage research paper state"
  default_prompt: "Use $mary-workflow to understand a project, plan and run milestones, or manage independent research-paper state with /mw-paper."

policy:
  allow_implicit_invocation: true
