interface:
  display_name: "Mary Workflow"
  short_description: "Run milestone-based project workflows"
  default_prompt: "Use $mary-workflow to initialize, plan milestones, and run a local workflow."

policy:
  allow_implicit_invocation: true
