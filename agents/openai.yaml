interface:
  display_name: "Mary Workflow"
  short_description: "Run milestones and create grounded paper summaries"
  default_prompt: "Use $mary-workflow to close-read and summarize an arXiv paper with machine-validated source evidence."

policy:
  allow_implicit_invocation: true
