interface:
  display_name: "Mary Workflow"
  short_description: "Read papers and build grounded research slides"
  default_prompt: "Use $mary-workflow to turn a completed paper summary into grounded ShanghaiTech Marp group-meeting slides."

policy:
  allow_implicit_invocation: true
