---
description: Compatibility alias for the Mary Workflow ExamPass review flow.
argument-hint: [course/exam/scope/date/mode]
---

# /mw-review

Use exactly the `/mw-exam` flow. The Mary renderer resolves `mw-review` to the same ExamPass prompt and shared Mary lifecycle. See `commands/mw-exam.md` and `skills/exam-review/SKILL.md`.
